# 客カード効果ディスパッチャ

●(GとPIDとカードと起点の)客カード効果実行とは
	カードの"effectId"が辞書キー存在をあるに代入。
	もしある=0ならば
		Gを戻す。
	ここまで。

	効果ID=カード["effectId"]

	# ここで effectId ごとに処理を分岐する。
	# まだカード能力一覧が未確定なので、現時点では受け口のみ用意する。
	もし効果ID=""ならば
		Gを戻す。
	ここまで。

	Gを戻す。
ここまで。
